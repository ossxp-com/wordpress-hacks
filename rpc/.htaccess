# BEGIN WordPress RPC
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /rpc/
RewriteCond %{REQUEST_URI} ^/?.+/.+
RewriteRule ^(/?.+)/([^/]+)(/.*)?$ $1$3?$2 [QSA]
</IfModule>

# END WordPress RPC
